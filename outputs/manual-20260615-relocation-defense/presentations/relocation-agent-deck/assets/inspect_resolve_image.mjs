import * as art from '@oai/artifact-tool';
console.log('resolveImageAssetPayload', art.resolveImageAssetPayload.toString().slice(0,1200).replace(/\n/g,' '));
console.log('FileBlob.load', art.FileBlob.load.toString().slice(0,600).replace(/\n/g,' '));
